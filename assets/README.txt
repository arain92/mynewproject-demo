Assets folder placeholder:
- Place your headshot as `headshot.jpg` if desired and update CSS/HTML to use it.
- Replace `favicon.svg` with your own logo/mark.
- Add `resume.pdf` to enable the Download Resume buttons.
- Add `og-image.png` for social previews.

Customization tips:
- Update name, bio, links in `index.html`.
- Edit colors in CSS variables under `[data-theme]` in `styles.css`.
- Chatbot responses live in `script.js` under `knowledge`.

Deployment:
- Serve statically (e.g., GitHub Pages, Netlify, Vercel). Root files required: `index.html`, `styles.css`, `script.js`, and `assets/*`.